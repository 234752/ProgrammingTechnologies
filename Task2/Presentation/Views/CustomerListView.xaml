<UserControl x:Class="Presentation.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Views"
             xmlns:VM="clr-namespace:Presentation.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <VM:CustomerListViewModel />
    </UserControl.DataContext>
    <Grid>
        <ListView d:ItemsSource="{Binding Customers}" Margin="28,188,48,51">
            <ListView.View>
                <GridView ColumnHeaderToolTip="id">
                    <GridViewColumn DisplayMemberBinding="{Binding}">
                        
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
