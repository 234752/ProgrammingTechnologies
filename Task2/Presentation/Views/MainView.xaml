<Window x:Class="Presentation.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation.View"
        xmlns:VM="clr-namespace:Presentation.ViewModel"
        xmlns:VIEW="clr-namespace:Presentation.Views"
        mc:Ignorable="d"
        Title="MainView" Height="700" Width="1200">
    <Window.DataContext>
        <VM:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid Margin="100,30,100,342" >
            <ContentControl Content="{Binding InnerView}"  />
        </Grid>
        <Grid Margin="100,342,100,95" >
            <ContentControl Content="{Binding InnerView2}"  />
        </Grid>

    </Grid>
</Window>
